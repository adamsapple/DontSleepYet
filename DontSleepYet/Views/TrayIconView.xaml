<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="DontSleepYet.Views.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DontSleepYet.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d">

    <tb:TaskbarIcon x:Name="notifyIcon"
                    x:Uid="notifyIcon"
                    >
        <!--<tb:TaskbarIcon.IconSource>
            <tb:GeneratedIconSource
            Text="❤️"
            Foreground="Red"
            />
        </tb:TaskbarIcon.IconSource>-->
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem
                    Text="Show/Hide Window"
                    Command="{x:Bind ShowHideWindowCommand}"
                    />
                <MenuFlyoutItem
                    x:Uid="UpdateCheckMenuItem"
                    Command="{x:Bind UpdateCheckCommand}"
                    />
                <ToggleMenuFlyoutItem
                    x:Uid="IsDisableLogoff_ToggleItem"
                    IsChecked="{x:Bind LocalSettingsOptions.IsDontSleepActive, Mode=TwoWay}"
                    Command="{x:Bind ToggleDontSleepActiveCommand}"
                    />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    x:Uid="LaunchTetheringConfig"
                    Command="{x:Bind LaunchTetheringConfigCommand}"
                    />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    Text="Exit"
                    Command="{x:Bind ExitApplicationCommand}"
                    />
                <!--Command="{Binding ElementName=window, Path=ExitApplicationCommand}"-->
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>

</UserControl>
